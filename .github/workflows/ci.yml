name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  brew:
    name: brew
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
          - runner: macos-15-intel
    steps:
      - uses: actions/checkout@v7

      - name: Tap local checkout
        run: brew tap loonfs/tap "$GITHUB_WORKSPACE"

      - name: Trust formula
        run: brew trust --formula loonfs/tap/loonfs

      - name: Audit formula
        run: brew audit --strict loonfs/tap/loonfs

      - name: Install formula
        run: brew install loonfs/tap/loonfs

      - name: Test formula
        run: brew test loonfs/tap/loonfs
